plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.jetbrains.kotlin.android)
}


android {
    compileSdkVersion = 34

    namespace("nl.isaac.androidlibs.uart_msgpacker")
    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    java {
        toolchain {
            JavaLanguageVersion.of(17)
        }
    }
    
    compileOptions {
        targetCompatibility = JavaVersion.VERSION_17
        sourceCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = '17'
    }
}

java {
    toolchain {
        JavaLanguageVersion.of(17)
    }
}

dependencies {
    // for git submodule library dependencies the version catalog doesn't offer a good solution. The overarching project would need to define our dependencies in their libs.versions.toml
    //noinspection UseTomlInstead
    implementation 'org.msgpack:msgpack-core:0.8.22'
    //noinspection UseTomlInstead
    implementation 'com.google.code.gson:gson:2.11.0'
    //noinspection UseTomlInstead
    testImplementation 'junit:junit:4.13.2'
    //noinspection UseTomlInstead
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    //noinspection UseTomlInstead
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}